const ZERO=/[\u200B-\u200F\u202A-\u202E\u2060-\u2064\uFEFF]/gu;
const ROMAN=/[ⅠⅡⅢⅣⅤⅥⅦⅧⅨⅩⅪⅫⅬⅭⅮⅯⅰⅱⅲⅳⅴⅵⅶⅷⅸⅹⅺⅻⅼⅽⅾⅿ]/u;
const CIRCLED=/[①②③④⑤⑥⑦⑧⑨⑩⑴⑵⑶⑷⑸⑹⑺⑻⑼⑽⒈⒉⒊⒋⒌⒍⒎⒏⒐⒑]/u;
const STORY_GROUP=/(?:【\s*(?:群提示|群消息|群公告)|聊天群(?:系统|任务|公告)|群成员|群友|群主(?:说|问|回复)|加入聊天群|^[\s　]*[^：:\n]{1,20}[：:][“"])/u;
const DOMAIN=/\b(?:EDG|LPL|LCK|SKT|RNG|IG|WE|OMG|TES|JDG|BLG|LOL|MVP|SMVP|ADC|TOP|SOLO|BP|MSI|AG|BO\d|S\d{1,2}|Letme|TheShy|Faker|Scout|mouse|iboy|meiko|clearlove)\b/iu;
const PLATFORM=/(?:求鲜花|求月票|求评价票|求收藏|求打赏|求推荐票|求催更|投鲜花|投月票)/gu;
const STRONG=new Set(Array.from('弍貳陾迩侕爾倭鸸洱栮贰玖镹泗児亻俬祁岜飼琦玐鳍陕衤瘤柒坝鹨氿捌叁轳咝鸠罒揪吆叄镏遛芭翏崎鏾覇壹陵紦磷硫傘陸熘厁韭锍澪掺彡氵焐邬裠箘逡麇踆帬峮羣囷宭鷗儛'));
const NUMBER_WORDS=['ling','yi','yao','er','liang','san','si','wu','liu','qi','ba','jiu'];
const SEP='[\\s\\p{P}\\p{S}\\u200B-\\u200F\\u202A-\\u202E\\u2060-\\u2064\\uFEFF]*';
const CONF={a:'[aAаɑ]',b:'[bB]',e:'[eEе]',g:'[gG]',i:'[iIіІıɩ]',j:'[jJ]',l:'[lLⅼӏ]',m:'[mM]',n:'[nN]',o:'[oOοо0]',q:'[qQ]',r:'[rR]',s:'[sS]',u:'[uU]',w:'[wW]',y:'[yY]'};
function fuzzyWord(word){return `(?<![A-Za-z])${word.split('').map(c=>CONF[c]||c).join(SEP)}(?![A-Za-z])`}
const BUILTIN_FAMILY_DEFS=[
  {kind:'family',name:'ling_meng',rx:new RegExp(`(?:${fuzzyWord('ling')}${SEP}(?:${fuzzyWord('meng')}|[梦夢])|[灵凌玲铃陵靈鈴零]${SEP}${fuzzyWord('meng')})`,'giu')},
  {kind:'family',name:'yue_yi',rx:new RegExp(`(?:${fuzzyWord('yue')}${SEP}(?:${fuzzyWord('yi')}|漪)|月${SEP}${fuzzyWord('yi')})`,'giu')},
  {kind:'family',name:'ruo_shui',rx:new RegExp(`(?:${fuzzyWord('ruo')}${SEP}(?:${fuzzyWord('shui')}|水)|若${SEP}${fuzzyWord('shui')})`,'giu')},
  {kind:'family',name:'ruo_xi',rx:new RegExp(`(?:${fuzzyWord('ruo')}${SEP}(?:${fuzzyWord('xi')}|曦)|若${SEP}${fuzzyWord('xi')})`,'giu')}
];
function escapeRx(s){return String(s).replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}
function aliasPattern(alias){const parts=String(alias||'').trim().match(/[A-Za-z]{2,12}|[\u3400-\u9fff]/gu)||[];if(!parts.length)return'';return parts.map(x=>/^[A-Za-z]+$/.test(x)?fuzzyWord(x.toLowerCase()):escapeRx(x)).join(SEP)}
function tokenDefsFor(rules={}){const defs=[...BUILTIN_FAMILY_DEFS];for(const fam of rules.adPinyinFamilies||[]){if(!fam||fam.enabled===false)continue;for(const alias of fam.aliases||[]){const pat=aliasPattern(alias);if(!pat)continue;try{defs.push({kind:'family',name:fam.id||fam.name||String(alias),rx:new RegExp(pat,'giu'),personal:true})}catch{}}}for(const fam of rules.adFamilies||[]){if(!fam||fam.enabled===false)continue;for(const alias of fam.pinyin||[]){const pat=aliasPattern(alias);if(!pat)continue;try{defs.push({kind:'family',name:fam.id||fam.name||String(alias),rx:new RegExp(pat,'giu'),personal:true})}catch{}}}defs.push({kind:'qun',name:'qun',rx:new RegExp(fuzzyWord('qun'),'giu')},...NUMBER_WORDS.map(x=>({kind:'num',name:x,rx:new RegExp(fuzzyWord(x),'giu')})));return defs}
function punctCount(s){return Array.from(s).filter(c=>/[\p{P}\p{S}]/u.test(c)).length}
function specialCount(s){return Array.from(s).filter(c=>ROMAN.test(c)||CIRCLED.test(c)).length}
function strongCount(s){return Array.from(s).filter(c=>STRONG.has(c)||/[\p{Cf}\p{So}]/u.test(c)).length}
function digitCount(s){return (s.match(/[0-9０-９]/gu)||[]).length}
function matchObj(lineNo,line,a,b,score,action,reason,kind='ad_pinyin_pollution',meta={}){return{line:lineNo,start:a,end:b,kind,score:+score.toFixed(2),field:line.slice(a,b),action,reason,originalLine:line,meta}}
function collectTokens(line,rules={}){const out=[];for(const d of tokenDefsFor(rules)){d.rx.lastIndex=0;for(const m of line.matchAll(d.rx)){if(!m[0])continue;out.push({kind:d.kind,name:d.name,start:m.index,end:m.index+m[0].length})}}for(const m of line.matchAll(/[群羣裙]/gu))out.push({kind:'qun',name:'group_han',start:m.index,end:m.index+m[0].length});out.sort((a,b)=>(a.kind==='family'?-1:0)-(b.kind==='family'?-1:0)||a.start-b.start||b.end-a.end);const familySpans=out.filter(x=>x.kind==='family'),ded=[];for(const t of out){if(t.kind==='num'&&familySpans.some(f=>t.start>=f.start&&t.end<=f.end))continue;if(!ded.some(x=>x.kind===t.kind&&x.start===t.start&&x.end===t.end))ded.push(t)}return ded.sort((a,b)=>a.start-b.start||b.end-a.end)}
function clusters(tokens){if(!tokens.length)return[];const out=[];let cur=[tokens[0]];for(const t of tokens.slice(1)){const prev=cur[cur.length-1];if(t.start-prev.end<=56)cur.push(t);else{out.push(cur);cur=[t]}}out.push(cur);return out}
function scoreCluster(line,ts){const a=Math.min(...ts.map(x=>x.start)),b=Math.max(...ts.map(x=>x.end)),core=line.slice(a,b),family=ts.some(x=>x.kind==='family'),qun=ts.some(x=>x.kind==='qun'),num=ts.filter(x=>x.kind==='num').length,digit=digitCount(core),special=specialCount(core),strong=strongCount(core),punct=punctCount(core);let score=0;if(family)score+=6;if(qun)score+=5;if(num>=2)score+=4;if(num>=4)score+=3;if(num>=1&&(digit+special+strong)>=2)score+=3;if(num>=1&&special>=2)score+=3;if(special)score+=2;if(strong>=2)score+=3;if(punct>=4)score+=1.5;if(family&&(qun||num||digit||special||strong))score+=2;if(STORY_GROUP.test(line)&&!family&&num<2&&digit===0&&special===0&&strong===0)score-=7;if(DOMAIN.test(line)&&!family&&!qun)score-=5;const high=(family&&(qun||num>=1||digit+special+strong>=2))||(qun&&num>=2)||(num>=3&&(special+strong+digit>=1))||(num>=1&&special+strong>=3),review=(family||(qun&&(num>=1||special+strong>=2))||num>=2||(num>=1&&special>=2))&&score>=5;return{score,family,qun,num,digit,special,strong,punct,high,review}}
function noisy(c){return /[\s\p{P}\p{S}0-9０-９]/u.test(c)||STRONG.has(c)||ROMAN.test(c)||CIRCLED.test(c)||/[\p{Cf}\p{So}]/u.test(c)}
function expandIsland(line,a,b){let x=a,y=b;while(x>0&&a-x<14&&noisy(line[x-1]))x--;while(y<line.length&&y-b<20&&noisy(line[y]))y++;return[x,y]}
function safeIslands(line,ts){const sorted=[...ts].sort((a,b)=>a.start-b.start),out=[];let cur=[sorted[0]];for(const t of sorted.slice(1)){const p=cur[cur.length-1],gap=line.slice(p.end,t.start);if(gap&&/[\u3400-\u9fff]/u.test(gap)&&!Array.from(gap).every(c=>noisy(c))){out.push(cur);cur=[t]}else if(t.start-p.end>20){out.push(cur);cur=[t]}else cur.push(t)}out.push(cur);return out.map(g=>{let a=Math.min(...g.map(x=>x.start)),b=Math.max(...g.map(x=>x.end));[a,b]=expandIsland(line,a,b);return[a,b]})}
function hasNormalHanBridge(s){return /[\u3400-\u9fff]{3,}/u.test(String(s||'').replace(/[群羣裙梦夢靈灵凌玲铃陵]/gu,''))}
function strongAdLine(line){return /(?:https?:\/\/|www\.|QQ群?|群号|微信|vx|V信|免费外群|中转群|公众号|防失联|找书神器|\b\d{7,12}\b)/iu.test(line)}
function genericGroupField(s){return /^(?:群|羣|裙|群聊|交流群|聊天群|群组|群主|群友)$/u.test(String(s||'').trim())}
function storyProtected(m,line){if(!STORY_GROUP.test(line)||strongAdLine(line))return false;if(m.kind==='full_line_ad')return true;if(m.kind==='learned_exact'&&genericGroupField(m.field))return true;return false}
function platformMatches(text){const lines=String(text||'').split(/\r?\n/),counts=new Map();for(const line of lines){const s=line.trim();if(s)counts.set(s,(counts.get(s)||0)+1)}const out=[];lines.forEach((line,i)=>{PLATFORM.lastIndex=0;const hits=[...line.matchAll(PLATFORM)];if(!hits.length)return;let residue=line;for(const h of hits)residue=residue.replace(h[0],'');const bare=residue.replace(/[\s\p{P}\p{S}0-9０-９]/gu,''),standalone=bare.length<=2||((counts.get(line.trim())||0)>=3&&line.trim().length<=160);if(standalone){out.push(matchObj(i+1,line,0,line.length,14,'delete','平台残留：独立/高频章节模板','platform_artifact'));return}for(const h of hits)out.push(matchObj(i+1,line,h.index,h.index+h[0].length,6,'review','平台词出现在正文中，仅复核该词，不删除整行','platform_artifact'))});return out}
function sanitizeExisting(result,text){const lines=String(text||'').split(/\r?\n/),auto=[],review=[],push=m=>(m.action==='delete'?auto:review).push(m);for(const m of[...(result?.auto||[]),...(result?.review||[])]){const line=lines[m.line-1]||m.originalLine||'';if(m.kind==='ad_pinyin_pollution'||m.kind==='platform_artifact')continue;if(storyProtected(m,line))continue;if(m.kind==='distributed_pollution'&&m.action==='delete'&&hasNormalHanBridge(m.field)){push({...m,action:'review',score:Math.min(m.score,6.9),reason:'多污染岛只用于关联提置信；检测范围含正常正文，强制改为复核，禁止整段自动删除'});continue}push(m)}for(const m of platformMatches(text))push(m);return{auto,review}}
export function detectAdPinyin(text,rules={}){const out=[],lines=String(text||'').replace(ZERO,'').split(/\r?\n/);lines.forEach((line,i)=>{const ts=collectTokens(line,rules);for(const c of clusters(ts)){const e=scoreCluster(line,c);if(!e.high&&!e.review)continue;const action=e.high&&e.score>=8?'delete':'review',reason=`拼音化广告优先检测：${e.family?'广告主体 ':''}${e.qun?'Qun/群 ':''}${e.num?`数字拼音×${e.num} `:''}${e.special?'圈号/罗马数字 ':''}${e.strong?'乱码混写 ':''}；仅删除污染岛，不跨正常正文`,meta={families:[...new Set(c.filter(x=>x.kind==='family').map(x=>x.name))],numberPinyin:c.filter(x=>x.kind==='num').map(x=>x.name),qun:e.qun};for(const[a,b]of safeIslands(line,c))out.push(matchObj(i+1,line,a,b,e.score,action,reason,'ad_pinyin_pollution',meta))}});return out}
function overlaps(a,b){return a.line===b.line&&!(a.end<=b.start||b.end<=a.start)}
function dedupe(ms){const out=[];for(const m of ms.sort((a,b)=>a.line-b.line||a.start-b.start||b.score-a.score)){const i=out.findIndex(x=>overlaps(x,m));if(i<0){out.push(m);continue}const x=out[i];if((m.action==='delete'?2:1)>(x.action==='delete'?2:1)||m.score>x.score)out[i]=m}return out.sort((a,b)=>a.line-b.line||a.start-b.start)}
export function mergeAdPinyinScan(result,text,rules,applyMatches,preserveBlank=true){const safe=sanitizeExisting(result,text),base=[...safe.auto,...safe.review],added=[];for(const m of detectAdPinyin(text,rules)){if(base.some(x=>overlaps(x,m)))continue;added.push(m)}const all=dedupe([...base,...added]),auto=all.filter(x=>x.action==='delete'),review=all.filter(x=>x.action==='review');return{...result,auto,review,cleaned:applyMatches(text,auto,preserveBlank),stats:{...(result?.stats||{}),auto:auto.length,review:review.length,adPinyinOverlay:all.filter(x=>x.kind==='ad_pinyin_pollution').length,spanSafety:true}}}
export function gatePinyinResult(text,result,rules={}){const suspicious=new Map();for(const m of detectAdPinyin(text,rules))if(m.score>=5)suspicious.set(m.line,m.reason);if(!suspicious.size)return result;const original=String(text||'').split(/\r?\n/),produced=String(result?.text||text).split(/\r?\n/);for(const[line]of suspicious)if(line>=1&&line<=produced.length)produced[line-1]=original[line-1];const auto=(result?.auto||[]).filter(x=>!suspicious.has(x.line)),review=(result?.review||[]).filter(x=>!suspicious.has(x.line)),blocked=[...(result?.blocked||[])];for(const[line,reason]of suspicious)if(!blocked.some(x=>x.line===line))blocked.push({line,reason:'广告拼音门控：'+reason,context:(original[line-1]||'').slice(0,180)});return{...result,text:produced.join('\n'),auto,review,blocked}}
